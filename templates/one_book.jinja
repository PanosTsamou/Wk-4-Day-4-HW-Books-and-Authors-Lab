{% extends "base.jinja" %}

{% block content %}
    <div>
        <h3>{{book.title}}</h3>
        <hr>
        <p> Author: {{book.author.name}}</p>
        <p>Genre: {{book.genre}}</p>
        <p><a href="/books/{{book.id}}/edit">Edit</a></p>
  </div>

{% endblock content %}  
